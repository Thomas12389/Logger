/*************************************************************************
	> File Name: test.cpp
	> Author: 
	> Mail: 
	> Created Time: Fri 20 Nov 2020 06:33:44 PM CST
 ************************************************************************/

#include <iostream>
#include "DevConfig/ParseConfiguration.h"
#include "ConvertData/ConvertData.h"
using namespace std;

extern std::string g_SeqNum;
extern Channel_MAP g_Channel_MAP;

int main() {
    const char *str1 = "./hw.xml";
    parse_configuration(str1);
    cout << "parse_xml ok" << endl;

    Channel_MAP::iterator Itr = g_Channel_MAP.begin();
    for (; Itr != g_Channel_MAP.end(); Itr++) {
        std::cout << Itr->first << std::endl;
    }
    std::cout << g_SeqNum << std::endl;

    std::cout << std::endl;

//    uint8_t msg[8] =  {0x00, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x08};
//    can_convert2msg("CAN1", 0x0A, msg);
//    can_convert2msg("CAN1", 0x7A0, msg);
//    can_convert2msg("CAN1", 0x7BD, msg);
//    can_convert2msg("CAN2", 0x7A0, msg);
    return 0;
}
